/*
Leaf nodes are external nodes.
What is the maximum number of leaf nodes in the k level of a binary tree/binary search tree?
How many leaf nodes at level k?
Also, I forgot how the same global variables could run different places at the same time. 
For example, for level k in function leveltraversal(), it adds up when the level goes down.
For level k in creating a tree, it should be renamed because in might introduce the value k from the leveltraversal().
If we have a global value k for levels, the operation of k should be in main().
*/
------------------------------
int main(){
 node *myTree;
 myTree.leveltraversal(e, 15);
 myTree.leveltraversal(e, 14);
 myTree.leveltraversal(e, 13);
 myTree.leveltraversal(e, 12);
//find the k level nodes using level traveral with an array
//expected tp print only the 3rd level with only one node,
//k=3 sum=1
}

struct node{
int data;
node *next;
}

void leveltraversal(node *child, int value){
int k; //counting the height of the tree
int sum=0;
//traverse all the levels
node *child=new node();
while(node!=NULL)
{ 
return k=k+1; counting the height with the level
return sum=sum+1; counting the number of nodes for each level
node->next;
}
}

node tree(node *root, int value){
int k;
k=0;
k=k+1;
for k=0 to k=k+1;
node *element=new node();
arr[]={k}
}

